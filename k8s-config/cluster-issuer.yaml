apiVersion: cert-manager.io/v1
kind: ClusterIssuer

metadata:
  name: letsencrypt-prod

spec:
  acme:
    email: danielrodriguesdrs331@gmail.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: cert-issuer-prod-key
    solvers:
      - dns01:
          route53:
            region: us-east-2
            hostedZoneID: Z03041063IRMFCQ8ILS25
