apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.app }}
  labels:
    app: {{ .Values.app }}

spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.app }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
